# Tasks

- [x] 1. Add task-binding lifecycle event helper for `session_start` and `task_bind`.
- [x] 2. Persist only short-lived `task_id`, `expires_at`, and `binding_confidence` in OMX active-session state.
- [x] 3. Keep first prompt summaries safe and bounded.
- [x] 4. Add tests for session-start binding, first-prompt binding, ambiguous/no-active candidates, and expired cache.
- [x] 5. Run focused tests and OpenSpec validation.
- [ ] 6. Finish via PR/merge/cleanup.
